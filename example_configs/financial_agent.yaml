name: financial_agent
allowed_imports:
  - financial_api
allowed_functions:
  # as an example we only want our agent to be able to create playlists of songs without podcasts
   - pay_bills
   - order_uber
   - other_money_transaction
allowed_arguments:
  - "<$50"
  
