name: secure_sandbox
blacklist_import:
  - os
  - sys
  - code
blacklist_functions:
  - open
blacklist_statements:
   # no try statements allowed
  - try 
no_exec: false # do you want the sandbox to not to continue execution if it found an escape?
