name: spotify
allowed_imports:
  -  spotify
allowed_functions:
  # as an example we only want our agent to be able to create playlists of songs without podcasts
   - create_playlist
   - add_song_to_playlist
   - delete_song_from_playlist